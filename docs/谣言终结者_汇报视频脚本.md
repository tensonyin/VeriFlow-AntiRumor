# 《谣言终结者 (VeriFlow-AntiRumor)》比赛汇报视频脚本

本脚本专为第九届全国青少年人工智能创新挑战赛 - AI智能体设计开发专项赛决赛视频汇报制作。脚本采用**“画面（视觉展示）- 旁白（配音）- 音效与BGM（听觉体验）”**三维立体结构，重点突出项目的**硬核技术方案**、**系统创新点**与**人文关怀属性**。

---

## 🎬 视频基本信息
* **视频定位**：比赛路演视频 / 决赛演示视频
* **视频时长**：约 5 - 6 分钟
* **核心调性**：前段具有科技感与紧迫感，中段突出技术硬核与系统架构，后段展现适老化的温情与社会价值。
* **背景音乐 (BGM)**：
  * **第一阶段 (00:00 - 00:45)**：低沉、带有悬疑与科技节奏感的音乐，营造谣言泛滥的紧迫氛围。
  * **第二阶段 (00:45 - 03:30)**：节奏轻快、充满科技感与未来感的电子乐，适合讲解系统架构和技术方案。
  * **第三阶段 (03:30 - 05:00)**：温馨、舒缓且充满温情的弦乐，突出适老设计与科技向善。
  * **第四阶段 (05:00 - 结束)**：激昂、高亢的收尾音乐，强调项目的总结与愿景。

---

## 📺 分镜脚本正文

| 时间 (估算) | 模块与画面描述 (Visual) | 配音旁白 (Voiceover) | 音效与BGM (SFX & Music) |
| :--- | :--- | :--- | :--- |
| **00:00 - 00:30** | **【痛点切入：AI时代的“虚假海啸”】**<br>• 画面1：微信朋友圈中各种“伪科学”、“防癌秘方”的虚假文章快速翻动。<br>• 画面2：展示大模型在回答某些求证时产生“胡言乱语”或“生成虚假链接”的拼贴图。<br>• 画面3：一位戴老花镜的银发老人在模糊、字体极小的手机屏幕前，显得无助与困惑。 | 在生成式 AI 爆发的今天，虚假信息的传播速度和制造门槛正以几何级数狂飙。而我们信任的通用大模型，又常常因为“事实幻觉”产生胡言乱语，成为谣言的二次催化剂。<br><br>在这场信息风暴中，视力退化、信息识别力弱的“数字银发族”，成了最易受到网络谣言、套路贷和伪科学常识侵害的弱势群体。 | **【BGM】**：低沉、压抑的科技悬疑背景音。<br>**【SFX】**：重音敲击，伴随刺耳的“信息过载”杂音。 |
| **00:30 - 00:55** | **【引出项目：谣言终结者登场】**<br>• 画面4：画面中央出现产品 Logo——**VeriFlow-AntiRumor (谣言终结者)**。<br>• 画面5：一键切换，展示系统双模状态。主界面从“极客极简科技模式”丝滑过渡到“温暖饱满的长辈大字模式”。<br>• 画面6：快速闪现：行空板物理终端、网页端、后台 23 节点工作流。 | 关怀银发，重塑数字信任边界。我们设计了——**《谣言终结者：基于多源异构对抗博弈的多模态事实核查系统》**。<br><br>这是一个将前沿多智能体协同、软硬一体化嵌入与极致适老化设计相结合的可信事实核查平台。 | **【BGM】**：音乐突然过渡到节奏清晰、科技感强的上升旋律。<br>**【SFX】**：科技启动音效（Whoosh），代表真相的机械重音。 |
| **00:55 - 01:40** | **【架构揭秘：23节点多智能体工作流管线】**<br>• 画面7：展示 Dify 后端工作流画布全景，动态数据流在 23 个节点间流转的炫酷连线效果。<br>• 画面8：局部放大并动画演示 **“特征提取与感知” -> “双轨并行（取证Agent与逻辑审计节点）” -> “材料充分性校验”** 的递进流转。<br>• 画面9：特写展示红蓝对抗的**“真相法庭”**博弈：左侧 Forensic Agent 调取 Tavily 跨境检索证据，右侧 Logic Judgment 挑刺逻辑漏洞，中央裁判长智能体定性裁决并分流生成 Mermaid 拓扑图。 | 为确保查证严谨，《谣言终结者》在后端构建了多达 **23 个节点** 的多智能体级联核查工作流。整个管线包含五个核心阶段：<br>第一，**多模态特征感知**，智能提取音视频与图文特征；<br>第二，**双轨并行取证与审计**，事实核查取证智能体自主翻译并跨境检索，同时逻辑漏洞节点对文本进行挑刺；<br>第三，**充分性校验**，动态研判证据链是否完整，防止片面定性；<br>第四，**‘真相法庭’对抗博弈**，通过红蓝交锋，由裁判长智能体做出公正裁决并动态输出 Mermaid 推导图；<br>第五，**合规修正与分流**，由合规 Agent 接收数据进行死链清洗，最终分流输出。 | **【BGM】**：平稳的科技感键盘打击声背景乐。<br>**【SFX】**：数据流穿梭的“嗖嗖”电子音效。 |
| **01:40 - 02:25** | **【硬核突破一：合规修正与死链检测】**<br>• 画面10：展示初版报告中出现的死链（红字警告），随后切入后端 **Compliance Agent** 运行状态。<br>• 画面11：展示 Dify 内置 Python 代码解释器 (Code Interpreter) 正在沙箱中并发执行 HTTP 请求测试 URL 连通性的代码细节。<br>• 画面12：显示代码执行完毕后，死链被自动过滤，报告重新排版，死链无痕替换为纯事实描述或“已过滤失效链接”标记。 | 针对行业内大模型引证时频发的“死链幻觉”，我们首创了 **Agent 闭环自我纠错机制**。<br><br>**报告合规修正专家 (Compliance Agent)** 自动调用内置 **代码解释器**，运行 Python 脚本并发测试所有引用链接。一旦检测到死链，该 Agent 立即自我反思重构，无痕修正报告，确保最终交付给用户的报告引用 100% 真实可用。 | **【BGM】**：富有逻辑感的数字处理旋律。<br>**【SFX】**：键盘打字声、代码编译成功的“叮”声。 |
| **02:25 - 03:00** | **【硬核突破二：Canvas 跨域污染与推导图交互】**<br>• 画面13：展示 Express 后端 `/api/proxy-image` 中转代理服务的代码，演示图片 URL 转换过程。<br>• 画面14：前端演示：用户在页面中点击“保存小票”，无论是普通模式还是长辈模式，包含网络图片的 DOM 卡片被瞬间捕获并顺利下载为无瑕疵的长图。<br>• 画面15：展示 Mermaid 拓扑推导图画布，演示用户在屏幕上双指捏合缩放、拖拽画布的流畅交互。 | 在前端，我们攻克了多项体验与技术瓶颈：普通与长辈模式下皆可保存辟谣小票长图，通过 Express 搭建**同源图片代理服务**，硬核化解了 Canvas 截图远程图片的跨域限制；<br><br>基于 d3-zoom 理念，我们为复杂的 **Mermaid 推导逻辑图** 开发了高精度拖拽缩放画布，支持长辈模式下字体自动放大 1.5 倍且不重叠堆叠，让推导过程“白盒化”清晰可见。 | **【BGM】**：轻快且富有成就感的节奏。<br>**【SFX】**：相机快门声（截图导出成功），以及水滴般的拖拽缩放音效。 |
| **03:00 - 03:30** | **【硬核突破三：行空板物理智能终端适配】**<br>• 画面16：展示部署在行空板 (Unihiker) 上的 `unihiker_app.py` Python 程序运行。<br>• 画面17：特写演示 PC 仿真模拟器界面，通过敲击键盘 A/B 键完美模拟硬件物理按键。<br>• 画面18：展示 Emoji 字符过滤装饰器的代码，以及在行空板上自动静默 pip 安装 requests、edge-tts 和 flask 的引导逻辑。 | 此外，系统实现了**软硬一体化嵌入**。在行空板物理终端开发中，我们编写了模块化的 Python 引导程序，实现了**自适应依赖引导安装**；<br><br>为了防止 Tkinter 遇到表情符号直接闪退，我们编写了 **Emoji 字符过滤装饰器**，并设计了**高保真 PC 仿真模拟器**，将硬件调试效率提升了数倍，打通了物理终端落地的最后一公里。 | **【BGM】**：硬件组装与嵌入式开发的工业电子风。<br>**【SFX】**：行空板蜂鸣器“滴滴”声。 |
| **03:30 - 04:15** | **【适老关怀：数字鸿沟的温情桥梁与语音选择】**<br>• 画面19：展示长辈模式下的大字极简界面。<br>• 画面20：展示等待查证期间，复杂的代码日志流被完全隐藏，取而代之的是一条**高可见度绿色进度条**，极大地安抚老人的空白等待焦虑。<br>• 画面21：结论生成，一枚大红色判定印章清脆盖下，页面弹出 **“温情儿女声 TTS 播报控制面板”**，用户可自由选择“儿子声”或“女儿声”双音色进行语音合成播报。 | 科技的终点是人性的温情。在长辈模式中，我们不仅放大了字号与行距，更引入了极简的**绿色图形化进度条**代替大堆复杂代码日志，物理与心理上安抚老年人的未知等待焦虑；<br><br>查证完成，拟物化小票盖下印章，还**新增了儿子声和女儿声的温情 TTS 语音播报选择**，让老人在听觉上能感受到亲人般的陪伴与贴心提醒。 | **【BGM】**：过渡到温暖、充满人文关怀的舒缓弦乐。<br>**【SFX】**：绿色进度条流转完毕 -> 沉稳清脆的盖章声“咚！” -> 温馨暖人声播报起。 |
| **04:15 - 04:45** | **【裂变阻断：一键分享朋友圈与 LaTeX 大字报】**<br>• 画面22：点击“一键生成分享卡片”，生成普通与长辈模式皆支持的极简“防骗辟谣小票长图”。<br>• 画面23：展示**长辈模式特有的一键生成 LaTeX 大字报海报**，红头大字排版精致，配以健康养生科普顺口溜和温情健康祝福。 | 辟谣不仅要快，更要阻断二次传播。普通模式与长辈模式下皆可一键导出微信辟谣小票；<br><br>而长辈模式更是**独家支持一键生成 LaTeX 辟谣大字报海报**。长辈可以随手转发给家族群和朋友圈，用长辈最容易接受的、符合传统阅读直觉的视觉载体，构筑家庭微信群的“流言防护网”。 | **【BGM】**：温情且充满力量的旋律。<br>**【SFX】**：微信消息发送成功的提示音。 |
---

## 💡 视频拍摄与剪辑建议

### 1. 声音与音效的运用 (SFX)
* **重中之重**：视频中必须包含**打字机印刷声**和**盖章声**。在画面展示“辟谣小票生成”和“盖章”时，这两个音效需要与画面动作完美同步，这能让评委隔着屏幕感受到该系统特有的实体交互仪式感。
* **背景配音 (VO)**：建议挑选一位声音沉稳、语速适中（约每分钟 230-250 字）、字正腔圆的成员进行配音，普通话标准，饱含情感，尤其是后半段的适老化人文关怀部分，应读出温度。

### 2. 录屏与实物演示 (Screencast & Live Demo)
* **系统白盒演示**：演示中一定要有 Dify 25节点工作流的真实动态展示，以及前端“思维风暴”中运行日志流式展开的特写，证实“白盒化”的推导可信。
* **行空板物理实体**：物理硬件演示段，应拍摄手部按压行空板按键启动录音、屏幕状态改变、行空板喇叭/音箱播放 edge-tts 语音的真实视频画面，证明项目的“软硬一体化”不是概念，而是已经高保真落地的物理成果。

### 3. 视觉包装 (Visual Packaging)
* 突出**对比效果**：通过分屏或者滑动对比，直观展现“普通模式”和“长辈大字模式”下，CSS 布局（字体放大1.5倍、高对比度、粗边框）的极致优化。
* 重点代码展示：在展示 Python Code Interpreter 运行、Canvas 跨域代理、Emoji 过滤函数时，代码块应使用高对比度暗黑主题，并用高亮框圈出核心硬核逻辑，展现团队的技术深度。

---

## 🛠️ 后端工作流节点技术详解 (基于 Dify Workflow YAML 配置)

为了方便视频制作时的技术特写、旁白深化以及评委提问准备，以下根据 [谣言终结者：基于多源异构对抗博弈的多模态事实核查系统 (12).yml](file:///d:/Desktop/谣言终结者/dify_workflows/谣言终结者：基于多源异构对抗博弈的多模态事实核查系统%20(12).yml) 提供了口语化 live 演示旁白与各核心节点的详细运行机制：

### 🎤 后端工作流 Live 演示旁白 (技术细节口语化配音)

> [!TIP]
> 录制后台 Dify 工作流实况（如展示节点流转或做系统演示视频）时，可直接使用以下口语化旁白作为配音或演示者解说：

* **口语化旁白内容**：
  “下面来看我们的后端工作流部分，这整套在 Dify 后端搭建的工作流，一共包含 23 个节点，也是我们系统的『硬核大脑』。
  
  首先，当用户通过web前端或行空板扔给系统一段文字、一张聊天截图、或者一小段语音时，工作流的**【用户求助 (Help Request)】开始节点**就会被瞬间激活。它不仅接收传言，还会拿到当前是不是『长辈模式』的布尔值变量。
  
  紧接着，数据被送往**【特征提取 (Content Sensing)】节点**。这里我们调用了 Gemini-3.5-Flash，开启视觉大模型的高清模式。不管你上传的是截图还是音视频，它都能第一步清点物理特征——比如做 OCR 提取文本，甚至清点画面里有没有 AI 深度伪造留下的边缘畸变，直接拿到了原始特征数据。
  
  特征拿到后，工作流启动了最精彩的**『双轨博弈对抗』**：
  在上方轨道，**【逻辑漏洞检查 (Logic Judgment)】节点**扮演『反方辩友』，纯粹靠逻辑和常识，死磕文本里有没有偷换概念、恐吓套路和常识硬伤，把里面的圈套全部揪出来；
  在下方轨道，**【事实核查取证智能体 (Forensic Agent)】**扮演『正方调查员』，它会调用 Tavily 搜索引擎。如果发现传言涉及海外或前沿科学，它还会自发地把中文关键词翻译成英文，并加上 `hoax`（骗局）或 `debunk`（辟谣）这类后缀，直接在全球范围发起跨境二次检索，抓取最权威的发布主体、定性和链接。
  
  当两边的报告都出来后，数据流向**【材料充分性校验】分支**。如果发现两边都是空的、数据根本不够，就会走**【异常输出 (Exception Output)】**，温馨拦截并提示用户补充材料。如果材料充足，则立刻将网传原话、网络证据和反方逻辑审查报告三合一，送入**【多源内容比对 (Cross-Verification)】**做多维度比对对齐。
  
  随后，**【定性裁决 (Final Judge)】大法官节点**闪亮登场。它会启动我们的核心判定算法，综合对齐矩阵和时效性，做出不偏不倚的『证实』、『伪造』或『存疑』终审定性。
  
  得出结论后，通过**【裁决结果分流 (Verdict Routing)】**，工作流在生成相应报告的同时，会触发**【流程图生成 (Mermaid Generator)】节点**，根据事件性质，动态地渲染出一张可手势缩放的 Mermaid 拓扑图。
  
  然后，这些初步报告会被**【变量聚合器 (Variable Aggregator)】**打包，送给最关键的安全关卡——**【报告合规修正专家 (Compliance Agent)】**。它会在内置的代码解释器里，跑一段 Python 脚本，用多线程测试报告中所有引用链接的可用性。一旦抓到 404 或失效死链，不仅会打标签过滤掉，还会自我反思、把报告重写一遍，保证呈现给用户的报告 100% 真实、无死链幻觉。
  
  最后，来到**【用户模式判断 (User Mode Check)】**。如果是长辈模式，系统会一气呵成地在下游生成两样东西：一个是专为 TTS 合成设计的**【安心报告】**，用儿女般的唠家常口吻把真相读给长辈听；另一个是直接输出纯 LaTeX 源码的**【LaTeX大字报】**节点，配合前端渲染成红头大字的喜庆防骗宣传海报。
  
  从多模态感知到双轨博弈，从代码解释器去死链到 LaTeX 海报大字报的输出，这整套严密的 23 节点工作流，就是《谣言终结者》高可信、极致适老背后的秘密武器！”

---

### 🔍 核心节点运行机制详解 (节点技术参考)

### 1. 收到用户求助 Help Request
* **节点类型**：开始节点 (`start`)
* **工作方式**：工作流的唯一启动入口。负责接收外界输入的求助信号与全局配置变量。
* **输入参数**：
  * `user_text` (段落/文本)：用户输入的求助传言或描述。
  * `upload_files` (文件列表)：用户通过物理终端或 Web 端上传的多模态媒体文件（支持 `.png`, `.jpg`, `.mp3`, `.mp4` 等文件，限 5 个）。
  * `isElderlyMode` (文本)：标识当前客户端是否开启长辈模式（`"true"` / `"false"`）。
* **输出变量**：直接输出上述三个变量（`user_text`、`upload_files`、`isElderlyMode`）至下游感知层与判断层节点。

### 2. 特征提取 Content Sensing
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：多模态数据预处理与特征清点。该节点开启视觉能力（High 模式），严格对输入的文件进行 OCR、视频字幕提取与物理特征清点（如音频的机械感、画面的 AI 合成畸变及线条扭曲等）。设置媒介合规红线，禁止无中生有和主观推导。
* **输入参数**：
  * 文本上下文：`Help Request.user_text`
  * 视觉输入：`Help Request.upload_files`
* **输出变量**：输出结构化 Markdown 特征报告，包含以下四部分：
  * `【提取核心正文】`
  * `【提及核心主体】`
  * `【文本风格与高频词特征】`
  * `【多模态物理特征检测报告】`（音频/图像物理检测细节）

### 3. 事实核查取证智能体 Forensic Agent
* **节点类型**：智能体节点 (`agent`) | 驱动模型：`gemini-3.5-flash` | 插件工具：`Tavily Search`
* **工作方式**：系统核心的“正方调查员”。
  1. 剥离煽动情绪词，提炼核心实体并生成最多 3 个简中关键词，调用 `Tavily Search` 检索国内权威核查结论。
  2. **跨境检索决策**：根据事件背景判定是否具备海外信源或前沿科技属性，若是则自动翻译为英文关键词并追加 `hoax`, `debunk`, `fact check` 等后缀，在国际互联网上发起二次跨境检索。
  3. 过滤并输出统一格式的结构化论据。
* **输入参数**：`Content Sensing.text` (提取到的核心文本)
* **输出变量**：格式化 Markdown 证据链：
  * `[技术核查要素解析]` (实体、简中检索词、跨境检索词)
  * `[权威取证证据链]` (包含发布主体、发布时间、事实定性、原始网页 URL 链接、可信度评级)

### 4. 逻辑漏洞检查 Logic Judgment
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：系统核心的“反方辩友”。不进行联网检索，纯粹从逻辑学、修辞学和生活常识角度对输入文本进行“逻辑死磕”与文本拆解。重点扫描：逻辑谬误与因果断层、恐吓焦虑情绪煽动、违背常识的物理/理化设定、剂量/边界条件缺失等。
* **输入参数**：`Content Sensing.text`
* **输出变量**：结构化“挑刺”报告（🔍 逻辑漏洞/修辞圈套 1/2/3）。若输入为无意义字符，则输出为空。

### 5. 材料充分性校验 Material Sufficiency Verification
* **节点类型**：条件分支节点 (`if-else`)
* **工作方式**：双轨证据链门禁。检测取证智能体与逻辑审计节点是否都有有效输出。如果二者都不为空，则放行进入最终定性；否则拦截并分流。
* **输入参数**：
  * 条件一：`Forensic Agent.text` (取证报告) 不能为空 (`not empty`)
  * 条件二：`Logic Judgment.text` (逻辑审计报告) 不能为空 (`not empty`)
* **输出变量**：根据逻辑 `and` 返回 `true` 分支（材料充足）或 `false` 分支（材料不足）。

### 6. 异常输出（材料不足） Exception Output (Insufficient Data)
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：安全拦截网关。当用户请求过于简短或缺乏核心实体（导致 `if-else` 走 `false` 分支）时，生成温馨且有科技温度的系统拦截引导，阻止系统进行无效的博弈消耗。
* **输入参数**：`Help Request.user_text`
* **输出变量**：输出极简的异常拦截公告（包含阻断状态声明、技术原因和行动建议）。下游接 `Insufficiency Out` 结束节点。

### 7. 多源内容比对 Cross-Verification
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：真相法庭的“预审官”。将原传言文本、正方网络搜索证据、反方逻辑漏洞检查报告三方数据对齐，执行“防污染独立审计原则”（核实搜索结果的时效性，修正早期媒体的断章取义或已过时乌龙），输出差异化对齐矩阵。
* **输入参数**：
  * `Content Sensing.text` (原传言)
  * `Forensic Agent.text` (网络证据)
  * `Logic Judgment.text` (反方报告)
* **输出变量**：比对矩阵 Markdown：
  * `比对矩阵 1：核心说法冲突` (原文说辞、最终真相、差异定性)
  * `比对矩阵 2：要素与边界核对` (时间、空间、剂量等漏洞)
  * `比对矩阵 3：圈套评估` (误导手法与证据保质期)

### 8. 定性裁决 Final Judge
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：真相法庭的“首席大法官”。负责给出最终的黑白灰三色裁决。
  1. 纠偏反方偏见（防无脑证伪）与纠偏正方盲信（防过时乌龙）。
  2. 遵循严密的定性规则判定为 **证实** / **伪造** / **存疑**。
* **输入参数**：
  * `Content Sensing.text` (网传谣言)
  * `Forensic Agent.text` (取证链)
  * `Logic Judgment.text` (反方报告)
  * `Cross-Verification.text` (比对矩阵)
* **输出变量**：首行为精准的结论标签（`证实` / `伪造` / `存疑`），第二行开始为 150 字以内的客观裁决理由。

### 9. 裁决结果分流 Verdict Routing
* **节点类型**：条件分支节点 (`if-else`)
* **工作方式**：根据大法官判定结果分流报告生成逻辑。
* **分流条件**：
  * 分支一：`Final Judge.text` 包含 `"伪造"` -> 流向 **辟谣报告生成**。
  * 分支二：`Final Judge.text` 包含 `"证实"` -> 流向 **证实报告生成**。
  * 分支三：`Final Judge.text` 包含 `"存疑"` -> 流向 **存疑报告生成**。

### 10. 报告生成层 (辟谣/证实/存疑报告生成)
* **节点类型**：大语言模型节点 (`llm`)
* **工作方式**：总编撰写阶段。严格遵循三段式规范进行输出：
  * **辟谣/伪造报告 (Rumour Report Generator)**：结论先行（定性） -> 真相解密（科学原理与真实数据） -> 漏洞拆解（逻辑圈套与恐吓心理）。
  * **证实报告 (Proved Report Generator)**：结论先行（证实） -> 真相解密（官方通报/来龙去脉） -> 行动指南（防范或后续应对）。
  * **存疑报告 (Doubtful Report Generator_REPORT)**：结论先行（悬决不确定） -> 现状对齐（已知边界与数据空白） -> 风险提示（防防反转/不信谣建议）。
* **输入参数**：`Final Judge.text` + `Forensic Agent.text` + `Logic Judgment.text`
* **输出变量**：未经脱敏的初版事实核查报告文本。

### 11. 流程图生成 Mermaid Generator
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：根据审判和对齐数据，动态生成一段 Mermaid（`graph TD`）源码。
  * **证实**：流向汇聚结构（通过加粗实线指向结论）。
  * **伪造**：分栏对比错位结构（左侧网传伪造链，右侧真实溯源链，红色虚线横向连线对比）。
  * **存疑**：发散多叉悬决结构（核心焦点居中，虚线分支指向未查明/带问号的悬空节点）。
  * 强制约束：禁止使用 `()` `[]` `""` 避免 MathJax 编译失败，并在底部使用 `style` 渲染莫兰迪配色方案（绿/红/黄）。
* **输入参数**：`Content Sensing.text` + `Cross-Verification.text` + `Logic Judgment.text` + `Final Judge.text`
* **输出变量**：包裹在 ````mermaid` 代码块中的流程图源码。下游直接通过 `Mermaid Out` 节点返回给客户端渲染。

### 12. 变量聚合器 Variable Aggregator
* **节点类型**：变量聚合节点 (`variable-aggregator`)
* **工作方式**：多合一变量漏斗。将辟谣、证实、存疑三条不同生成分支的报告文本合拢，输出给相同的字符串变量，以便合规修正专家节点能够统一接收处理。
* **输入参数**：`辟谣报告.text` 或 `证实报告.text` 或 `存疑报告.text`
* **输出变量**：聚合后的统一报告文本 `text`。

### 13. 报告合规修正专家 Compliance Agent
* **节点类型**：智能体节点 (`agent`) | 驱动模型：`gemini-3.5-flash` | 插件工具：`代码解释器`
* **工作方式**：质量合规把关人。
  1. 接收聚合后的报告，利用正则匹配出报告内所有的 HTTP/HTTPS 链接。
  2. 调用 Dify 内置 `Code Interpreter` 并发运行一段 Python 脚本，向这些 URL 发送 HEAD 或 GET 请求测试连通性。
  3. 过滤并使用 `[已过滤失效链接]` 替代 404 或超时的失效死链。
  4. **自我纠错重构**：若包含死链，触发自我修正案，在大模型侧无缝重写，自然抹除死链和占位符痕迹，只保留 100% 可达的干净证据引用。
* **输入参数**：`Variable Aggregator.text`
* **输出变量**：合规修正后的最终 100% 真实无死链的 Markdown 事实核查报告。

### 14. 用户模式判断 User Mode Check
* **节点类型**：条件分支节点 (`if-else`)
* **工作方式**：检查全局标志位。
  * `true` 分支：开启适老化流程，流向 **安心报告生成** 与 **LaTex大字报生成**。
  * `false` 分支：结束流，直接输出常规报告。
* **输入参数**：`Help Request.isElderlyMode`

  * `true` 分支的流向：
    * 安心有声广播文本 -> 发送至 **安心报告生成 Elderly Report Generation** 节点，再流向 **安心报告输出** 结束节点。
    * LaTeX 源码 -> 发送至 **LaTex大字报生成 LaTex Poster Generation** 节点，再流向 **LaTex大字报输出** 结束节点。

### 15. 安心报告生成 Elderly Report Generation
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：适老化有声广播的核心生成节点。
  1. 接收 `Compliance Agent` 修正后的事实核查报告。
  2. 转换人设为亲切、温馨的“社区健康电台广播员”，进行适合长辈的“降维”转述。
  3. 严格遵循三段式结构：【第一段：开篇定心丸】（直接给结论安抚情绪） -> 【第二段：聊天式科普】（用接地气的生活常识和打比方代替小票中晦涩的科技词汇与引用死链） -> 【第三段：贴心老友叮嘱】（顺口溜或温情大白话结尾）。
* **输入参数**：`Compliance Agent.text` (ID `1782555770708.text`)
* **输出变量**：通俗易懂、适合语音播报的安心有声广播文本（`text`）。下游流向 **安心报告输出** 节点。

### 16. 安心报告输出
* **节点类型**：结束节点 (`end`)
* **工作方式**：将生成的有声广播文本流式或全量返回给客户端（Web端或行空板物理终端），供 Edge-TTS 合成语音并由扬声器进行降速播报。
* **输入参数**：`Elderly Report Generation.text` (ID `1782465366127.text`)

### 17. LaTex大字报生成 LaTex Poster Generation
* **节点类型**：大语言模型节点 (`llm`) | 驱动模型：`gemini-3.5-flash`
* **工作方式**：大字报海报的排版源码生成节点。
  1. 接收 `Compliance Agent` 修正后的事实核查报告。
  2. 将结论、科普、祝福整合，排版为醒目的、无实线边框、使用虚线（`\hdashline`）自然分隔的“三段式大字报”。
  3. **LaTeX 语法约束**：最外层使用双美元符号 `$$` 包裹；汉字和标点之间必须强制插入“`\ `”或“`．`”手动隔开，防止 MathJax/KaTeX 渲染时中文汉字堆叠；大字报使用 `\begin{array}{c} ... \end{array}` 纵向排列。
* **输入参数**：`Compliance Agent.text` (ID `1782555770708.text`)
* **输出变量**：纯净的 LaTeX 大字报源码（`text`）。下游流向 **LaTex大字报输出** 节点。

### 18. LaTex大字报输出
* **节点类型**：结束节点 (`end`)
* **工作方式**：将生成的 LaTeX 代码输出给前端 Web 客户端，用于高保真大字辟谣海报的实时渲染与长图下载。
* **输入参数**：`LaTex Poster Generation.text` (ID `1782470849360.text`)

---

## 🖥️ 前端 Web 与行空板物理终端技术详解

为了让路演视频技术表述更加突出重点，我们将“普通模式”作为前端的主线演示，并引入“长辈模式”与“行空板物理终端”作为特定适老场景下的核心补充。以下是该演示逻辑下的口语化旁白及各端的技术特色分析：

### 🎤 页面交互 Live 演示旁白 (普通模式主线演示 + 适老双端补充)
为了让路演视频技术表述更加突出重点，我们将“普通模式”作为前端的主线演示，并引入“长辈模式”与“行空板物理终端”作为特定适老场景下的核心补充。以下是该演示逻辑下的口语化旁白及各端的技术特色分析：

### 🎤 页面交互 Live 演示旁白 (普通模式主线演示 + 适老双端补充)

> [!TIP]
> 录制视频演示或现场路演时，可直接使用以下口语化配音脚本：

* **口语化旁白内容**：
  “下面我来为大家展示《谣言终结者》的多端交互生态。我们**以『普通模式』作为主线演示**，并配合**『长辈模式』**和**『行空板硬件终端』**，展示系统对不同群体的差异化适配。
  
  首先，大家请看屏幕上这款**极简沙漠温馨风的 Web 主界面**。这就是系统默认的**『普通模式』**，主要面向普通大众和青少年。
  在这里，我们可以直接输入任何可疑言论，或者上传待查证内容的图片、音视频或文档，点击开始查证。
  
  查证启动后，页面右侧会即时拉起我们的**『思维风暴』**日志卡片。大家注意，它利用 SSE 流式传输，正在实时直播大模型后端 23 个节点的运行状态，以及特征提取、跨境取证、红蓝博弈和报告合规修正的全部细节。这种『白盒化推理』，把晦涩 Graves 完全透明地展现给用户，极大地增强了权威感和信任度。
  
  分析完成后，左侧直接输出了**排版干净、带有权威出处引用链接的专业 Markdown 辟谣报告小票**。同时，下方渲染出支持手势捏合缩放的 **Mermaid 逻辑拓扑推导图**，帮助年轻用户理清谣言的逻辑硬伤。
  
  但这套系统最温情的地方，在于我们**一键切换到针对银发族深度优化的『长辈模式』**。
  一键切换后，大家可以看到，所有的字号、边框和色彩对比度都瞬间重构，放大到了极致。
  
  更贴心的是，在等待查证期间，系统为了避免老年人看着大片代码日志产生无措感，将思维风暴模块简化为一条**高可见度的绿色图形化进度条**，极大地安抚了老人的空白等待焦虑！同时也省去了各节点的具体内容，避免长辈信息过载。
  
  查证完成后，长辈模式除了以更大字号呈现辟谣小票外，特别为长辈**增加了儿子声和女儿声的温情 TTS 语音播报选择**，让老人在听觉上能感受到亲人般的陪伴与提醒。此外，系统支持一键生成红头文件感的 **LaTeX 养生大字报海报**，以及各平台分享小票长图，极大地方便了老人在家庭微信群里阻断流言。
  
  最后，为满足完全不习惯用手机的老人，我们更延伸开发了**『行空板物理智能终端』**作为补充。
  硬件开机即用，老人按一下侧边的 **A 按键**即可启动麦克风录音求助，完全不需要接触触控屏。因为行空板上没有合适的物理扬声器，我们巧妙地通过中转，让**电脑 Web 界面充当扬声器**来播放声音，用放慢了一倍、儿女般温存的声音把报告结论大声读给老人听。
  
  以普通模式展示技术深度，以长辈模式与行空板展示极致的人文关怀。这就是我们多端协同设计背后的硬核实力！
  
  相比于通用的 ChatGPT 或 Claude，《谣言终结者》展现出了碾压性的对比优势：
  第一是**时效与可信度**：通用模型极易被静态知识库和网上虚假信源污染，产生事实幻觉；而我们依靠多智能体级联取证与红蓝博弈，只信铁证，彻底消除了幻觉；
  第二是**引用链接质量**：通用大模型经常给出不可达的虚假“死链”；而我们的合规专家智能体通过内置的 **Python 代码解释器**，自动清洗掉所有死链，并由大模型进行自我重构，保证引用的 URL 100% 真实可用；
  第三是**社交传播阻断力**：大模型冷冰冰的对话框无法阻断流言二次传播；而我们支持一键导出防跨域的朋友圈分享小票长图、LaTeX 红头养生大字报，以及儿子声和女儿声的温情陪伴 TTS 播报，用长辈最容易接受的视听载体阻断裂变。
  
  在 AI 技术狂飙突进的今天，我们的目标不仅是探索科技的硬核边界，更是用人性的温情驯化技术，为长辈重塑清朗、安全的网络空间！以科技微光，照亮真相之路。谢谢大家！”

---

### 🔍 三端功能差异与特色对比矩阵

| 对比维度 | 🖥️ Web端 - 普通模式 (主要演示 - 极客沙漠风) | 👵 Web端 - 长辈模式 (补充演示 - 温暖适老风) | 📟 硬件端 - 行空板物理终端 (补充演示 - 软硬一体) |
| :--- | :--- | :--- | :--- |
| **受众定位** | 青少年、极客、辟谣研究人员（通用求证窗口） | 银发族、视力障碍群体（网页适老窗口） | 居家“网关安全卫士”、不操作手机的长辈（硬件端） |
| **输入方式** | 文本输入、富文本、批量多模态文件拖拽 | 超大字体输入、显眼高亮语音按钮指引 | 侧边物理 A 键按键录音、一键一求助 |
| **交互调性** | 极简、温馨、数据流导向、高可信度白盒呈现 | 高对比度（粗边框）、暖黄底色、超大字号自适应放大 | 实体物理拟物反馈、板载蜂鸣器双向声光交互 |
| **等待焦虑控制** | **思维风暴直播**：流式全量卡片流输出 Tavily 检索过程与红蓝博弈细节，主打“高科技感” | **绿色进度条**：利用**绿色高可见度进度条**控制等待节奏，隐藏复杂日志，消除盲目等待焦虑 | **LCD 状态刷新**：硬件屏幕刷新当前运行阶段名，伴随蜂鸣器断续音物理指引 |
| **输出结论载体** | **Markdown 辟谣报告小票** + Mermaid 推导流程图（支持拖拽缩放与自适应） | **拟物化大字辟谣小票**（带盖章动效）+ **LaTeX 三段式养生大字报** + **温情儿女声双音色 TTS 语音广播选择** | **语音安心报告**（由于行空板无板载扬声器，通过网络中转由**电脑 Web 界面充当扬声器**进行播报） + **Emoji 过滤安全文本**大字展示 |
| **分享阻断设计** | 复制报告文本，适合网页端严肃求证 | **一键微信朋友圈长图卡片**（内置同源图片代理防 CORS 画布污染） | 摆放于家庭公共区域，随时通过喇叭阻断家庭谣言 |

---

### 🔍 前端 Web 核心组件技术参考

#### 1. 响应式布局与双模态切换 (`src/App.tsx`)
* **工作方式**：前端的控制器。维护全局状态机，包括 `input`（待查文本及文件流）、`mode`（模式切换 `isElderlyMode`）、`status`（空闲、获取中、分析中、展示中、出错）。通过 Server-Sent Events (SSE) 保持长连接，实时接收并拆包后端 Dify 推送 of 节点变更事件。
* **输入方式**：接收用户界面的交互操作（点击切换按钮、输入框变更、拖入截图等）。
* **输出方式**：改变全局 CSS 变量（如字体大小比例、线宽、背景莫兰迪色色值），并实时渲染子组件。

#### 2. 同源截图与防画布污染小票 (`src/components/ResultTicket.tsx`)
* **工作方式**：
  1. **跨域突破**：利用 `html2canvas` 导出长图时，若包含远程网络图片，浏览器会出于 CORS 保护锁死 canvas 读取。本组件通过正则将所有 Markdown 引证图片链接映射为 `/api/proxy-image?url=${encodeURIComponent(url)}`。请求由 Express 后端代理 fetch 并返回同源 Buffer，完美解决截图跨域限制。
  2. **视觉机制**：渲染热敏小票和带透明通道 of SVG 盖章盖印效果，并绑定盖章音效（SFX），提供真实的物理仪式感。

#### 3. 手势级拖拽缩放拓扑画布 (`src/components/MermaidChart.tsx`)
* **工作方式**：
  1. **防止文字堆叠**：针对长辈模式下放大 1.5 倍后的字体，组件通过动态 CSS 强制覆写 SVG 中的 text 样式并扩大 `letter-spacing`。
  2. **手势操作**：绑定 `mousedown`、`mousemove`、`mouseup` 以及鼠标滚轮 `wheel` 事件，计算平移向量与缩放系数，通过 `transform: translate(x,y) scale(s)` 的 CSS Matrix 直接应用于渲染后的 SVG 标签，实现流畅的双指捏合放大、拖动。

#### 4. SSE 白盒化日志流组件 (`src/components/ThinkingWorkflow.tsx`)
* **工作方式**：实时展示后台节点的流式状态。将接收到的每个 Dify 节点事件组织成“手风琴卡片”，在当前运行节点上绑定“打字机动画”，流式打印出检索日志或逻辑分析细节；一旦运行结束则自动折叠，仅保留状态勾选标志，保证界面干净。

---

### 🔍 行空板物理终端技术参考 (`unihiker_app.py`)

#### 1. 自适应依赖引导机制 (`install_dependencies`)
* **工作方式**：在行空板极简 Linux 固件环境下运行。初始化时遍历依赖清单（`requests`, `edge-tts`, `flask`），使用 `subprocess` 在当前 Python 执行路径下静默拉起 `pip install`，如检测到已安装则直接跳过，实现一键通电即用。

#### 2. 非 BMP 字符 (Emoji) 安全防崩溃装饰器
* **工作方式**：Tkinter 底层在尝试绘制大于 Unicode `0xffff` 的字符（如 🌟、❌、😅）时会发生底层致命闪退。本系统通过重写 `draw_text` 接口，在渲染文本送入 Tkinter 画布绘制前，用正则：
  `"".join(c for c in text if ord(c) <= 0xffff)`
  无痕清洗掉所有特殊 Emoji 字符，换以纯文本替代，避免了硬件层面的异常宕机。

#### 3. 环境检测与 Tkinter 仿真模拟器
* **工作方式**：在脚本头部捕获 `from unihiker import GUI` 的异常。
  * 若在行空板上，则调用物理 GUI 接口。
  * 若在开发机（Windows/Mac）上，则动态拉起一个 Tkinter 窗口，精确比例为 240x320 像素，监听 `Key-A` 和 `Key-B` 键盘事件模拟板载物理 A/B 键，并调用系统的 `cmd` 进程进行测试音频播放，实现软硬一体的离线高保真调试。

#### 4. 远程 Web 扬声器中转代理机制 (Speaker Proxy)
* **工作方式**：鉴于行空板板载硬件体积与环境限制，没有配备适合语音播报的板载喇叭。系统在 unihiker_app.py 终端内生成 Edge-TTS 音频请求后，将生成的 mp3 文件路径或数据流以网络请求形式推送到后端代理服务器（server/index.ts），并通过已连接的 React Web 前端进行降速、放大播放，巧妙地让电脑 Web 界面充当物理终端的“虚拟扬声器”，实现多端软硬联动的声音分发。
